# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual envs ---
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# --- Testing / coverage ---
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# --- Tooling caches ---
.ruff_cache/
.mypy_cache/
.pyre/
.pytype/

# --- Jupyter ---
.ipynb_checkpoints
*.ipynb

# --- Frontend (if a build step is ever added) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.parcel-cache/
dist-frontend/

# --- Env / secrets ---
.env
.env.*
!.env.example
*.secret
secrets/
credentials.json

# Bootstrap output — tenant-specific Anthropic Managed Agents resource IDs
managed_ids.json

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~
.zed/

# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# --- Logs / runtime ---
*.log
logs/
pids/
*.pid
*.seed

# --- Project-specific: hackathon anti-contamination ---
brain_dump_hackathon.md
notes_perso/
ref_repairmind/
scratch/
TODO.local.md

# --- Superpowers brainstorming (visual companion mockups) ---
.superpowers/

# --- Project-specific: artifacts ---
data/
downloads/
board_assets/*.pdf
!board_assets/.gitkeep
# Runtime data — technician's working data, never committed (any device, any brand)
memory/*
!memory/.gitkeep

# Evolve loop state — locally-managed per-machine, do not version
evolve/*
!evolve/.gitkeep
!evolve/reports
evolve/reports/*
!evolve/reports/.gitkeep

# --- Demo / offline fallback ---
# Vendored CDN assets pulled by `make pin-cdn` (D3, marked, DOMPurify).
# Regenerated on demand; not tracked.
web/vendor/*.js

# --- Local-only notes (gap analyses, prompts ad-hoc, working memos) ---
# Use docs/notes/local/ for any markdown that should NOT land in the repo
# history. Tracked architecture/design notes go directly in docs/notes/.
docs/notes/local/
